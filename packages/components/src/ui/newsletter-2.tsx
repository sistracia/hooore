import type { Newsletter2Props } from "../types/newsletter-2";

export function Newsletter2(
  props: Newsletter2Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
