import type { Faq4Props } from "../types/faq-4";

export function Faq4(
  props: Faq4Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
  },
) {
  const _ = props;

  return null;
}
