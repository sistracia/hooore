import type { Footer3Props } from "../types/footer-3";

export function Footer3(
  props: Footer3Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
