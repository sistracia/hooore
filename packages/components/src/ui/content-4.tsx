import type { Content4Props } from "../types/content-4";

export function Content4(
  props: Content4Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
  },
) {
  const _ = props;

  return null;
}
