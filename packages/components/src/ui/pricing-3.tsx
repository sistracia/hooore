import type { Pricing3Props } from "../types/pricing-3";

export function Pricing3(
  props: Pricing3Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
