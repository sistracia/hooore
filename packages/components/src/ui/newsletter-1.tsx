import type { Newsletter1Props } from "../types/newsletter-1";

export function Newsletter1(
  props: Newsletter1Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
