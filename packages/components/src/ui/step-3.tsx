import type { Step3Props } from "../types/step-3";

export function Step3(
  props: Step3Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
