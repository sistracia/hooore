import type { FeaturesList3Props } from "../types/features-list-3";

export function FeaturesList3(
  props: FeaturesList3Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
  },
) {
  const _ = props;

  return null;
}
