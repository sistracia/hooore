import type { Gallery2Props } from "../types/gallery-2";

export function Gallery2(
  props: Gallery2Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
  },
) {
  const _ = props;

  return null;
}
