import type { Stats2Props } from "../types/stats-2";

export function Stats2(
  props: Stats2Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
