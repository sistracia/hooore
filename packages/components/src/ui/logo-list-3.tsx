import type { LogoList3Props } from "../types/logo-list-3";

export function LogoList3(
  props: LogoList3Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
