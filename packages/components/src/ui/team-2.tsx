import type { Team2Props } from "../types/team-2";

export function Team2(
  props: Team2Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
