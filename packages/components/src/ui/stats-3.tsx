import type { Stats3Props } from "../types/stats-3";

export function Stats3(
  props: Stats3Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
