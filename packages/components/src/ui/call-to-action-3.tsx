import type { CallToAction3Props } from "../types/call-to-action-3";

export function CallToAction3(
  props: CallToAction3Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
