import type { Blog2Props } from "../types/blog-2";

export function Blog2(
  props: Blog2Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
