import type { Testimonials3Props } from "../types/testimonials-3";

export function Testimonials3(
  props: Testimonials3Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
