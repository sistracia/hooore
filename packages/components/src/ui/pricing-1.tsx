import type { Pricing1Props } from "../types/pricing-1";

export function Pricing1(
  props: Pricing1Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
