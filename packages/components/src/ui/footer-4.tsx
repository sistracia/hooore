import type { Footer4Props } from "../types/footer-4";

export function Footer4(
  props: Footer4Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
