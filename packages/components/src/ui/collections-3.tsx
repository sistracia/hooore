import type { Collections3Props } from "../types/collections-3";

export function Collections3(
  props: Collections3Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
