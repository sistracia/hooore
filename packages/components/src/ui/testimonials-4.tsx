import type { Testimonials4Props } from "../types/testimonials-4";

export function Testimonials4(
  props: Testimonials4Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
