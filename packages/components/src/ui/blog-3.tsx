import type { Blog3Props } from "../types/blog-3";

export function Blog3(
  props: Blog3Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
