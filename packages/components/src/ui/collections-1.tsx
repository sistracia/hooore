import type { Collections1Props } from "../types/collections-1";

export function Collections1(
  props: Collections1Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
  },
) {
  const _ = props;

  return null;
}
