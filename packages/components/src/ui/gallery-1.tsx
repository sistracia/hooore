import type { Gallery1Props } from "../types/gallery-1";

export function Gallery1(
  props: Gallery1Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
