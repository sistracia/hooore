import type { Team3Props } from "../types/team-3";

export function Team3(
  props: Team3Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
