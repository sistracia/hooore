import type { Step2Props } from "../types/step-2";

export function Step2(
  props: Step2Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
