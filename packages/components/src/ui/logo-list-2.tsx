import type { LogoList2Props } from "../types/logo-list-2";

export function LogoList2(
  props: LogoList2Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
