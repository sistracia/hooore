import type { Stats1Props } from "../types/stats-1";

export function Stats1(
  props: Stats1Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
