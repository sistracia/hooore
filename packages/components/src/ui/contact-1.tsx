import type { Contact1Props } from "../types/contact-1";

export function Contact1(
  props: Contact1Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
