import type { Contact2Props } from "../types/contact-2";

export function Contact2(
  props: Contact2Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
