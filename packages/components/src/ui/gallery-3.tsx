import type { Gallery3Props } from "../types/gallery-3";

export function Gallery3(
  props: Gallery3Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
  },
) {
  const _ = props;

  return null;
}
