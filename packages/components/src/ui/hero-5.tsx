import type { Hero5Props } from "../types/hero-5";

export function Hero5(
  props: Hero5Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
