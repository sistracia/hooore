import type { Hero4Props } from "../types/hero-4";

export function Hero4(
  props: Hero4Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
  },
) {
  const _ = props;

  return null;
}
