import type { LogoList4Props } from "../types/logo-list-4";

export function LogoList4(
  props: LogoList4Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
