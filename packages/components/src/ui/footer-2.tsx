import type { Footer2Props } from "../types/footer-2";

export function Footer2(
  props: Footer2Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
