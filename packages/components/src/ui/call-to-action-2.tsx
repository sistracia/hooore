import type { CallToAction2Props } from "../types/call-to-action-2";

export function CallToAction2(
  props: CallToAction2Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
