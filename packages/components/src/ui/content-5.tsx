import type { Content5Props } from "../types/content-5";

export function Content5(
  props: Content5Props & {
    disableAnimation?: boolean;
    disableLink?: boolean;
    logo?: string;
  },
) {
  const _ = props;

  return null;
}
