import type {
  Stats2Props,
  Stats2Slug,
} from "@repo/components/types/template-types/stats-2";
import type { FormFields } from "../types";

export const STATS_2_FORM_SCHEMA: FormFields<Stats2Slug, Stats2Props> = {
  slug: "stats-2",
  fields: [],
};
