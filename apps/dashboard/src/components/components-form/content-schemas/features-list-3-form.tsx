import type {
  FeaturesList3Props,
  FeaturesList3Slug,
} from "@repo/components/types/template-types/features-list-3";
import type { FormFields } from "../types";

export const FEATURES_LIST_3_FORM_SCHEMA: FormFields<
  FeaturesList3Slug,
  FeaturesList3Props
> = {
  slug: "features-list-3",
  fields: [],
};
