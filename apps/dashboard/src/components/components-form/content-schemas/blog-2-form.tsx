import type {
  Blog2Props,
  Blog2Slug,
} from "@repo/components/types/template-types/blog-2";
import type { FormFields } from "../types";

export const BLOG_2_FORM_SCHEMA: FormFields<Blog2Slug, Blog2Props> = {
  slug: "blog-2",
  fields: [],
};
