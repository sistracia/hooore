import type {
  Blog1Props,
  Blog1Slug,
} from "@repo/components/types/template-types/blog-1";
import type { FormFields } from "../types";

export const BLOG_1_FORM_SCHEMA: FormFields<Blog1Slug, Blog1Props> = {
  slug: "blog-1",
  fields: [],
};
