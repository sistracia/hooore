import type {
  Gallery3Props,
  Gallery3Slug,
} from "@repo/components/types/template-types/gallery-3";
import type { FormFields } from "../types";

export const GALLERY_3_FORM_SCHEMA: FormFields<Gallery3Slug, Gallery3Props> = {
  slug: "gallery-3",
  fields: [],
};
