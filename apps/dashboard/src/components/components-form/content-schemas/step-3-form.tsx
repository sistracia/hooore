import type {
  Step3Props,
  Step3Slug,
} from "@repo/components/types/template-types/step-3";
import type { FormFields } from "../types";

export const STEP_3_FORM_SCHEMA: FormFields<Step3Slug, Step3Props> = {
  slug: "step-3",
  fields: [],
};
