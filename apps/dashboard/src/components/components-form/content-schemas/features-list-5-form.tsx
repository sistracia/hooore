import type {
  FeaturesList5Props,
  FeaturesList5Slug,
} from "@repo/components/types/template-types/features-list-5";
import type { FormFields } from "../types";

export const FEATURES_LIST_5_FORM_SCHEMA: FormFields<
  FeaturesList5Slug,
  FeaturesList5Props
> = {
  slug: "features-list-5",
  fields: [],
};
