import type {
  Blog3Props,
  Blog3Slug,
} from "@repo/components/types/template-types/blog-3";
import type { FormFields } from "../types";

export const BLOG_3_FORM_SCHEMA: FormFields<Blog3Slug, Blog3Props> = {
  slug: "blog-3",
  fields: [],
};
