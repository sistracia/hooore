import type {
  Stats3Props,
  Stats3Slug,
} from "@repo/components/types/template-types/stats-3";
import type { FormFields } from "../types";

export const STATS_3_FORM_SCHEMA: FormFields<Stats3Slug, Stats3Props> = {
  slug: "stats-3",
  fields: [],
};
