import type {
  Contact2Props,
  Contact2Slug,
} from "@hooore/components/types/template-types/contact-2";
import type { FormFields } from "../types";

export const CONTACT_2_FORM_SCHEMA: FormFields<Contact2Slug, Contact2Props> = {
  slug: "contact-2",
  fields: [],
};
