import type {
  Newsletter1Props,
  Newsletter1Slug,
} from "@hooore/components/types/template-types/newsletter-1";
import type { FormFields } from "../types";

export const NEWSLETTER_1_FORM_SCHEMA: FormFields<
  Newsletter1Slug,
  Newsletter1Props
> = {
  slug: "newsletter-1",
  fields: [],
};
