import type {
  Contact1Props,
  Contact1Slug,
} from "@hooore/components/types/template-types/contact-1";
import type { FormFields } from "../types";

export const CONTACT_1_FORM_SCHEMA: FormFields<Contact1Slug, Contact1Props> = {
  slug: "contact-1",
  fields: [],
};
