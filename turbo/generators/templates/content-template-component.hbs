import type { {{pascalCase name}}Props } from "../../types/template-types/{{dashCase templateName}}";

export type {{pascalCase name}}RendererProps = {{pascalCase name}}Props & {
  disableAnimation?: boolean;
  disableLink?: boolean;
  logo?: string;
};

export function {{pascalCase name}}(props: {{pascalCase name}}RendererProps) {
  const _ = props;

  return null;
}
